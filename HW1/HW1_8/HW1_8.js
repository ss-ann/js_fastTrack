const sec = 1;
const min = 60 * sec;
const hour = 60 * min;
const day = 24 * hour;
const monthMarch = 31 * day; 

alert(`An hour has ${hour} seconds,\na day has ${day} seconds,\na month of March has ${monthMarch} seconds`);